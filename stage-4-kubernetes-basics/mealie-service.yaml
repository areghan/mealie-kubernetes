apiVersion: v1
kind: Service
metadata:
  name: mealie
  namespace: mealie
spec:
  type: NodePort

  selector:
    app: mealie

  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
      nodePort: 30080
